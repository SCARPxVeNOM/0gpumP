services:
  - type: web
    name: 0g-pump-streaming
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node streaming-server-simple.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 8000
